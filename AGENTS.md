# AGENTS.md — 开发安全规则

本文件约束所有在本仓库工作的人与 AI Agent。任何实现、重构、测试、文档或运维变更都必须遵守。

本项目是基于 **Hummingbot + Hyperliquid** 的单策略自动合约交易系统。错误下单会造成真实资金损失。默认假设：**尚未获得用户对真实交易的明确确认**。

## 硬性交易安全规则

下列规则不可谈判，不可被“方便实现”“测试更快”“临时绕过”覆盖。

1. **禁止策略直接下单。** 策略模块只做信号计算，唯一合法输出为 `LONG` / `SHORT` / `CLOSE` / `HOLD`。策略内不得调用交易所 API、Hummingbot connector、下单函数、撤单函数或钱包签名。
2. **禁止绕过 Trading Controller。** 所有开仓、平仓、撤单、紧急停止相关执行必须经过 Trading Controller。UI、脚本、定时任务、恢复流程、测试夹具都不得直接打到 Hummingbot / Hyperliquid。
3. **永远最多一个实际持仓。** 系统在任何时刻最多允许一个 Hyperliquid 永续持仓。不得拆单成多个方向、不得对冲、不得同时持有 LONG 与 SHORT。
4. **有持仓禁止新的 LONG / SHORT 开仓。** 只要交易所或 Recovery 认定存在持仓，Trading Controller 必须拒绝 `LONG` 和 `SHORT`。只允许 `HOLD` 或 `CLOSE`。
5. **禁止自动反手。** 不得把 `LONG`（在已有 SHORT 时）或 `SHORT`（在已有 LONG 时）解释为“先平后开”。反手必须拆成：先 `CLOSE` 完成，再在无持仓且非 Recovery 时，由后续独立信号开仓。
6. **Hyperliquid 真实状态优先于本地状态。** 仓位、挂单、余额、成交以交易所为准。本地 SQLite 只是缓存与审计。冲突时以 Hyperliquid 为准，并进入安全处理，不得用本地状态“纠正”交易所。
7. **Recovery 状态禁止开仓。** 进程启动、崩溃恢复、状态不一致、交易所查询失败、对账未完成时，系统必须进入 Recovery。Recovery 期间禁止 `LONG` / `SHORT`。只允许只读同步、必要的安全平仓（且仅当用户/控制面明确请求）和进入 `HOLD`。
8. **所有交易相关设置必须持久化。** 策略选择、策略版本、参数开关、交易币种、杠杆、下单数量、风控阈值、UI 控制状态（运行/停止）、紧急停止标志等必须写入 SQLite（或同等持久层），进程退出后仍在。禁止只放内存、只放临时文件、或重启丢失。
9. **禁止提交真实密钥。** 禁止将 Hyperliquid 私钥、API secret、钱包助记词、`.env` 实值、connector 密钥文件提交到 Git。示例文件只能用占位符。
10. **禁止未经确认进行真实交易。** 未得到用户在当前会话中的明确确认前，不得连接 Hyperliquid 主网、不得发送真实订单、不得用真实账户做“试一下”。开发默认：不连真实交易所，不下真实单。
11. **不允许通过修改测试来掩盖代码问题。** 禁止删除、跳过、放宽断言、改期望值、注入假成功，来让失败测试变绿。先修产品代码或测试夹具的真实性；测试必须保护上述安全规则。
12. **每个 Phase 完成必须生成报告并停止。** 完成本 Phase 目标后写入对应 `docs/PHASE_N_REPORT.md`（或该 Phase 指定路径），然后停止。未经用户明确要求，不得开始下一 Phase，不得“顺便”实现核心交易功能。

## 系统边界（必须保持）

| 组件 | 允许 | 禁止 |
| --- | --- | --- |
| Strategy | 输出 `LONG` / `SHORT` / `CLOSE` / `HOLD` | 下单、改仓、读密钥、直接访问 connector |
| Trading Controller | 校验信号、执行、风控、恢复、一键平仓 | 自动反手、在有仓时开新仓、Recovery 开仓 |
| Hummingbot | 作为执行层下单/撤单/查仓 | 成为策略决策者；被 UI 或其他模块旁路 |
| Web UI | 启动、停止、一键平仓并停止、一键平仓继续运行、紧急停止、策略上传与参数开关 | 自己拼交易所请求 |
| SQLite | 设置、策略版本、运行状态、审计日志 | 作为仓位真相源覆盖交易所 |

## 运行约束

- 单账户、单策略、单交易币种。
- 目标市场：Hyperliquid 永续合约。
- 部署形态：Docker Compose；需能在 Windows / macOS / Linux 开发与运行。
- 故障与重启后必须能恢复：先 Recovery 对账，再决定是否继续；恢复后不得自动开新仓。

## Web UI 控制面（执行层必须支持）

- 启动
- 停止
- 一键平仓并停止
- 一键平仓继续运行
- 紧急停止

紧急停止必须优先取消挂单并停止开仓；其精确行为以实现阶段设计为准，但不得违反“最多一个持仓 / 禁止自动反手 / Recovery 禁止开仓”。

## 密钥与环境

- 真实密钥只存在于本地未跟踪文件或 Docker secret，不进仓库。
- 未经用户确认，禁止对 Hyperliquid 主网做任何私有 API 调用。
- 后续接入优先 testnet / mock / paper；主网必须单独确认。

## 开发流程

- 不删除用户已有文件，除非用户明确要求。
- 本 Phase 未要求的功能不要实现。
- 修改交易路径时，必须同时考虑：有仓、无仓、Recovery、交易所与本地不一致、紧急停止。
- 需要浏览器验证 Web UI 时，验证行为而不只是截图。

## Phase 完成定义

一个 Phase 完成 = 该 Phase 目标已落地 + 指定报告已写入 + Agent 停止等待用户指示。
